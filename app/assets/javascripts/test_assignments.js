document.addEventListener('DOMContentLoaded', function(){
    (function () {
        // Enabled tabs
        $('.menu .item').tab();

        // Enabled cool checkboxes;
        $('.ui.checkbox').checkbox();
    }());
}, false);
